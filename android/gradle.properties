org.gradle.jvmargs=-Xmx2048M -XX:MaxMetaspaceSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true

# Enable Gradle build cache for faster builds
org.gradle.caching=true

# Enable parallel builds to utilize multiple CPU cores
org.gradle.parallel=true

# Configuration cache can improve build performance but is experimental
# Set to true after verifying builds work correctly with your project setup
org.gradle.configuration-cache=false

# Configure Gradle daemon for better performance
org.gradle.daemon=true
